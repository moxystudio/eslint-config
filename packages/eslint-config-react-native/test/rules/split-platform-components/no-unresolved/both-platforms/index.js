// `no-unresolved` - Ensure imports point to a file/module that can be resolved
// --------------------------------------------------------------------
// Good: should not fail when both `.ios.js` and `.android.js` modules exist
import MyComponent from './MyComponent';
