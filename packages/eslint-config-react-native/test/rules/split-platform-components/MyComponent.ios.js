// `split-platform-components` - Enforce using platform specific filenames when necessary
// --------------------------------------------------------------------
// Good: iOS-specific component being used in `.ios.js` file
import { ComponentIOS } from 'react-native';
