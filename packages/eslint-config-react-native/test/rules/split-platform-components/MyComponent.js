// `split-platform-components` - Enforce using platform specific filenames when necessary
// --------------------------------------------------------------------
// Bad: Don't mix Android and iOS-specific components in the same file
import { ComponentAndroid, ComponentIOS } from 'react-native';
