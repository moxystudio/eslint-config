'use strict';

// Made up sample.. not genuine

// `restricted-properties`
// Testing restricted properties

(function () {
    function pow(x) {
        return Math.pow(x, 2);
    }

    pow();
})();
